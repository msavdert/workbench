#!/usr/bin/env bash
# remote-ls   list Remote Control servers on this machine and their state.
set -euo pipefail
systemctl --user list-units --type=service --all --no-legend --no-pager 'claude-remote*' |
  awk '{printf "%-32s %-8s %s\n", $1, $3, $4}'
