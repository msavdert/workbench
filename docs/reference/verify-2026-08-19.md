# Captured `make provision STEPS=verify`, 2026-08-19

A transcript of the verification step against the running box (Proxmox
VM, rebuilt from scratch the same day: `make bootstrap-all` green in 3m14s,
47 checks ok, 0 failed). `verify` is the last step of every provision; this
capture is kept so the claim in README.md points at output, not prose. The
first three lines are the Makefile's own echo. Regenerate with
`make provision STEPS=verify > docs/reference/verify-<date>.md`.

```
rsync -az --delete --exclude .git box/ agent-vm-ssh:~/workbench-box/
ssh -o BatchMode=yes -t agent-vm-ssh "sudo ~/workbench-box/bootstrap.sh $STEPS"
Pseudo-terminal will not be allocated because stdin is not a terminal.
  ok   systemctl is-active docker
  ok   bash -c systemctl is-active ssh || systemctl is-active ssh.socket

[bootstrap] == verify
  ok   bash -c test ! -e /dev/virtio-ports/org.qemu.guest_agent.0 || systemctl is-active qemu-guest-agent
  ok   as_agent docker ps
  ok   as_agent bash -lc command -v mise
  ok   as_agent bash -lc command -v node
  ok   as_agent bash -lc command -v gh
  ok   as_agent bash -lc command -v op
  ok   as_agent bash -lc command -v uv
  ok   as_agent bash -lc command -v go
  ok   as_agent bash -lc command -v bun
  ok   as_agent bash -lc command -v omp
  ok   as_agent bash -lc command -v agy
  ok   as_agent bash -lc command -v herdr
  ok   as_agent bash -lc command -v claude
  ok   as_agent bash -lc command -v jq
  ok   as_agent bash -lc command -v rg
  ok   as_agent bash -lc command -v fd
  ok   as_agent bash -lc command -v tmux
  ok   as_agent bash -lc command -v docker
  ok   as_agent bash -lc sudo -n true
  ok   bash -c swapon --show=NAME --noheadings | grep -qx /swapfile
  ok   test -f /etc/needrestart/conf.d/99-agent.conf
  ok   test -L /home/agent/.claude/CLAUDE.md
  ok   grep -q boundary-gate.sh /home/agent/.claude/settings.json
  ok   test -x /home/agent/.claude/hooks/herdr-agent-state.sh
  ok   test -d /home/agent/.omp/agent/extensions
  ok   test -f /home/agent/.gemini/config/hooks.json
  ok   as_agent bash -lc '/home/agent/work/workbench/home/install.sh' --check box
  ok   as_agent bash -lc test "$MISE_ENV" = box
  ok   command -v zsh
  ok   test -f /etc/claude-code/CLAUDE.md

[bootstrap] == done
```
