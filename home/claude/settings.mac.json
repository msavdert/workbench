{
  "agentPushNotifEnabled": true,
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: PUBLIC — msavdert/workbench (github.com) — any push here publishes it",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: 1Password `op://` references resolved per command; never hardcode a secret or write a literal credential to a tracked file (per ~/.claude/CLAUDE.md operator preference)",
      "**Default / protected branches**: main (default); no rulesets or protected branches listed via gh for msavdert/workbench",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo (msavdert/workbench, github.com/msavdert/workbench) and its remote only — no additional orgs configured",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar; note repo's own .gitignore flags `.env` as sensitive",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development — personal infrastructure/platform tooling (workbench: VM bootstrap, agent configuration, provisioning)",
      "**Trusted repo**: msavdert/workbench (github.com/msavdert/workbench, public) — since public, only this repo's own work is OK to commit/push here; confidential material and secrets are never cleared into it by visibility alone",
      "**Org-specific CLIs**: None configured — repo Makefile targets observed (bootstrap-all, provision, remote-add/ls/rm, rollback, secrets, snapshot(s), vm-create/destroy/status/wait) are project-local, not org CLIs",
      "routine under msavdert/workbench prefix: git, mise, mac (repo-local tooling seen in this project's transcript)"
    ]
  },
  "hasCompletedOnboarding": true,
  "permissions": {
    "defaultMode": "auto"
  },
  "theme": "auto"
}
